Stack and queue
Example: Implement Queue using Stacks https://leetcode.com/problems/implement-queue-using-stacks/description/
class MyQueue:
    def __init__(self):
        self.stackA=[]
        self.stackB=[]
    def push(self, x: int) -> None:
        self.stackA.append(x)
        
    def pop(self) -> int:
        if not self.stackB:
            while self.stackA:
                value= self.stackA.pop()
                self.stackB.append(value)
        return self.stackB.pop()
   
    def peek(self) -> int:
        if not self.stackB:
            while self.stackA:
                value= self.stackA.pop()
                self.stackB.append(value)
        return self.stackB[-1]
   

    def empty(self) -> bool:
        return max(len(self.stackA),len(self.stackB))==0
        


# Your MyQueue object will be instantiated and called as such:
# obj = MyQueue()
# obj.push(x)
# param_2 = obj.pop()
# param_3 = obj.peek()
# param_4 = obj.empty()
