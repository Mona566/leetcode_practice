Hash Table 
Valid Anagram https://leetcode.com/problems/valid-anagram/description/
class Solution:
    def isAnagram(self, s: str, t: str) -> bool:
        if len(s)!=len(t):
            return False
        hashsetS,hashsetT= {},{}

        for i in range(len(s)):
            hashsetS[s[i]]=1+hashsetS.get(s[i],0)
            hashsetT[t[i]]=1+hashsetT.get(t[i],0)
        for i in hashsetS:
            if hashsetS[i]!=hashsetT.get(i,0):
                return False
        return True
Notice:
          
dict.get(key, default) is a Python method: It looks for key in the dictionary:
  If key exists → returns its value.
  If key does not exist → returns the default value you provide.
